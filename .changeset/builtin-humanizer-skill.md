---
"kilo-code": minor
---

Ship the Humanizer skill built into the CLI and apply its writing rules to all agent output so generated prose reads like a person wrote it instead of sounding AI-generated
