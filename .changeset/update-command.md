--
"@legion/cli": minor
--

Add legion update command to check for new versions from npm and update automatically. Adds timeout protection to version registry lookups and upgrade operations to prevent indefinite hangs on slow networks.
