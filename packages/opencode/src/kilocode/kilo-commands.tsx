/**
 * Kilo Commands for TUI (no-op after gateway removal)
 */

// These types are OpenCode-internal and imported at runtime
type UseSDK = any

/**
 * Register Kilo commands (currently empty after gateway removal)
 */
export function registerKiloCommands(_useSDK: () => UseSDK) {
  // No gateway commands to register
}
